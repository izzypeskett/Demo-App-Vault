import { PostServiceRequest } from '@meeco/vault-api-sdk';
import { AuthData } from '../models/auth-data';
import { Environment } from '../models/environment';
/**
 * Manage organizations from the API.
 */
export declare class OrganizationServicesService {
    private api;
    private cryppo;
    constructor(environment: Environment, vaultAccessToken: string);
    getLogin(organizationId: string, serviceId: string, privateKey: string): Promise<AuthData>;
    create(organizationId: string, service: PostServiceRequest): Promise<{
        service: import("@meeco/vault-api-sdk").Service;
        privateKey: any;
        publicKey: any;
    }>;
}
