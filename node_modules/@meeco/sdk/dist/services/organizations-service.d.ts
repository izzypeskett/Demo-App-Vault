import { PostOrganizationRequest } from '@meeco/vault-api-sdk';
import { AuthData } from '../models/auth-data';
import { Environment } from '../models/environment';
/**
 * Manage organizations from the API.
 */
export declare class OrganizationsService {
    private api;
    private cryppo;
    constructor(environment: Environment, vaultAccessToken: string);
    getLogin(id: string, privateKey: string): Promise<AuthData>;
    create(organization: PostOrganizationRequest): Promise<{
        organization: import("@meeco/vault-api-sdk").Organization;
        privateKey: any;
        publicKey: any;
    }>;
}
