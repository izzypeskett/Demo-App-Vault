import { ClientTaskQueueResponse } from '@meeco/vault-api-sdk';
import { Environment } from '../models/environment';
/**
 * A ClientTask represents a task the client is supposed to perform.
 */
export declare class ClientTaskQueueService {
    private vaultAPIFactory;
    constructor(environment: Environment);
    list(vaultAccessToken: string, supressChangingState?: boolean, state?: State): Promise<ClientTaskQueueResponse>;
    countOutstandingTasks(vaultAccessToken: string): Promise<IOutstandingClientTasks>;
}
export declare enum State {
    Todo = "todo",
    InProgress = "in_progress",
    Done = "done",
    Failed = "failed"
}
export interface IOutstandingClientTasks {
    todo: number;
    in_progress: number;
}
