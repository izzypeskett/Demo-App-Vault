import { AuthData } from './auth-data';
export interface IConnectionMetadata {
    toName: string;
    fromName: string;
}
export declare class ConnectionCreateData {
    from: AuthData;
    to: AuthData;
    options: IConnectionMetadata;
    constructor(from: AuthData, to: AuthData, options: IConnectionMetadata);
}
