import { Slot } from '@meeco/vault-api-sdk';
export declare class ItemUpdateData {
    id: string;
    label: string;
    slots?: Slot[];
    constructor(config: {
        id: string;
        slots?: Slot[];
        label: string;
    });
}
