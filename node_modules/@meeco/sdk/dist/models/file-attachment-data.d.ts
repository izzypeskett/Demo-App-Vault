/// <reference types="node" />
export declare class FileAttachmentData {
    /**
     * Id of the item to add the attachment to
     */
    readonly itemId: string;
    /**
     * Label of the slot that the attachment will be added to
     */
    readonly label: string;
    /**
     * The file to attach
     */
    readonly file: Buffer | Uint8Array | ArrayBuffer;
    /**
     * Name to be used for the file
     */
    readonly fileName: string;
    /**
     * MIME type of the file
     */
    readonly fileType: string;
    constructor(config: {
        /**
         * Id of the item to add the attachment to
         */
        itemId: string;
        /**
         * Label of the slot that the attachment will be added to
         */
        label: string;
        /**
         * Name to be used for the file
         */
        fileName: string;
        /**
         * MIME type of the file
         */
        fileType: string;
        /**
         * The file to attach
         */
        file: Buffer | Uint8Array | ArrayBuffer;
    });
}
